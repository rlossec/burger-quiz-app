/**
 * Configuration des polices Burger Quiz
 */

export const fonts = {
  heading: "'Syne', 'Unbounded', system-ui",
  body: "'DM Sans', system-ui",
} as const;

export type ThemeFonts = typeof fonts;
